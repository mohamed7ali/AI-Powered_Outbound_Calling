# Runtime dependencies.
# pyproject.toml is the source of truth — keep this file in sync with [project.dependencies].
# Install: pip install -r requirements.txt
# Or (preferred): uv pip install -e .

# Backend and UI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
gradio==5.35.0
sentence-transformers>=3.0.0,<6.0.0

# Authentication, PostgreSQL, Supabase REST, and pgvector
PyJWT[crypto]>=2.9.0
psycopg[binary,pool]>=3.2.0
pgvector>=0.3.6
requests>=2.32.0

# Gemini/OpenAI-compatible LLM and embeddings
openai>=1.55.0

# RAG/document ingestion
pypdf>=5.0.0
python-docx>=1.1.2

# Vonage and application runtime
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
httpx>=0.27.0
